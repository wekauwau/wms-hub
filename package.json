{
  "name": "wms-hub",
  "author": "wekauwau",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w packages/server\" \"npm run dev -w packages/web\"",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down -v",
    "db:migrate": "npm run migrate -w packages/server",
    "db:seed": "npm run seed -w packages/server",
    "lint": "npm run lint -w packages/server && npm run lint -w packages/web",
    "lint:fix": "npm run lint:fix -w packages/server && npm run lint:fix -w packages/web",
    "typecheck": "npm run typecheck -w packages/server && npm run typecheck -w packages/web",
    "format": "prettier --write \"packages/**/*.{ts,vue,json,css}\"",
    "format:check": "prettier --check \"packages/**/*.{ts,vue,json,css}\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
